; 语音控制项目（声音检测与拍手识别）
; 库：Adafruit SSD1306（根配置已含）
[platformio]
default_envs = esp32dev

[env:esp32dev]
extends = env:esp32dev
build_src_filter = +<*>
build_flags =
    -DBOARD_NAME="VoiceControl"
    -DCORE_DEBUG_LEVEL=0
