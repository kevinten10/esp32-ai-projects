; 智能家居控制项目
; 需要的库：WiFi（内置）、WebServer（内置）、Adafruit SSD1306（根配置已含）
[platformio]
default_envs = esp32dev

[env:esp32dev]
extends = env:esp32dev
build_src_filter = +<*>
build_flags =
    -DBOARD_NAME="SmartHome"
